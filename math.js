
const sum = (manUtd, Arsenal) => manUtd + Arsenal;
const sub = (manUtd, Arsenal) => manUtd - Arsenal;

const calculator = {
  multiply: (p, q) => {
    return p * q;
  },
};

module.exports = { sum, calculator, sub };